machine:
  services:
    - docker
dependencies:
  pre:
    - make ci-dependencies
    - sudo make sshcommand
    - sudo make pluginhook
    - sudo make stack
    - sudo make copyfiles
    - sudo make plugin-dependencies
    - sudo make version
test:
  override:
    - sudo make test
