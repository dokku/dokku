machine:
  services:
    - docker
dependencies:
  pre:
    - make ci-dependencies
    - for pkg in `dpkg --get-selections | grep hold | awk '{ print $1 }'`; do echo $pkg install | sudo dpkg --set-selections ; done
    - sudo -E make -e install
test:
  override:
    - sudo make test
