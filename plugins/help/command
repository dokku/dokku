#!/usr/bin/env bash
set -eo pipefail; [[ $DOKKU_TRACE ]] && set -x

for helpfile in $(ls -d $PLUGIN_PATH/*/help.txt); do
  PLUGIN=$(basename ${helpfile%/help.txt})
  sed -e "s/^\\s*: /$PLUGIN /" -e "s/^\\s*:/$PLUGIN:/" -e "s/^/    /" -e '$a\' $helpfile
done

# Print any legacy plugin help output
pluginhook commands help | sort
