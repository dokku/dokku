#!/usr/bin/env bash
source "$PLUGIN_AVAILABLE_PATH/common/functions"
set -eo pipefail
[[ $DOKKU_TRACE ]] && set -x

trigger-haproxy-backup-app-import() {
  declare desc="restores the app's haproxy properties from the backup scope dir"
  declare trigger="backup-app-import"
  declare APP="$1" SCOPE_DIR="$2"
  "$PLUGIN_AVAILABLE_PATH/backup/backup-property" import "haproxy" "$APP" "$SCOPE_DIR"
}

trigger-haproxy-backup-app-import "$@"
