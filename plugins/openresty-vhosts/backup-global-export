#!/usr/bin/env bash
source "$PLUGIN_AVAILABLE_PATH/common/functions"
set -eo pipefail
[[ $DOKKU_TRACE ]] && set -x

trigger-openresty-backup-global-export() {
  declare desc="exports the global openresty properties into the backup scope dir"
  declare trigger="backup-global-export"
  declare SCOPE_DIR="$1"
  "$PLUGIN_AVAILABLE_PATH/backup/backup-property" export "openresty" "--global" "$SCOPE_DIR"
}

trigger-openresty-backup-global-export "$@"
