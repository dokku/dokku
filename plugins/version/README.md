# Dokku core version command

This plugin provides the `version` command, which outputs the version of Dokku
that is installed, as stored as the content of the VERSION file in the Dokku
root directory.

