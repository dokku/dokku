#!/usr/bin/env bash
source "$PLUGIN_AVAILABLE_PATH/common/functions"
set -eo pipefail
[[ $DOKKU_TRACE ]] && set -x

trigger-builder-pack-backup-app-import() {
  declare desc="restores the app's builder-pack properties from the backup scope dir"
  declare trigger="backup-app-import"
  declare APP="$1" SCOPE_DIR="$2"
  "$PLUGIN_AVAILABLE_PATH/backup/backup-property" import "builder-pack" "$APP" "$SCOPE_DIR"
}

trigger-builder-pack-backup-app-import "$@"
