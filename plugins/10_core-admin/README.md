# Dokku core app administration commands

This plugin provides commands the `delete`, `logs`, `run`, and `url` commands
for managing apps. It also includes the hooks that specify the base plugin
deletion behavior.
